/* Figueroa Fernando DIV X*/
function mostrar()
{
	let numero;
	const MINIMO = 1;
	const MAXIMO = 10;
	numero = Math.round(Math.random() * (MAXIMO - MINIMO) + MINIMO);

	alert(numero);
}//FIN DE LA FUNCIÓN